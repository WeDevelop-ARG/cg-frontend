import React from 'react'

import classes from './styles.module.scss'

const DotSelected = () => {
  return (
    <button type='button' className={classes.dotSelected} />
  )
}

export default DotSelected
